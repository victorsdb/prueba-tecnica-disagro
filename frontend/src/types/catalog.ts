export interface Product {
    id: number;
    name: string;
    description?: string;
    price: string;
}

export interface Service {
    id: number;
    name: string;
    description?: string;
    price: string;
}